//
// Created by wannes on 31.03.19.
//

#include <gtest/gtest.h>

using namespace std;

namespace {

    TEST(workplacePopulatorTest, workplacePopulatorTest) {
        EXPECT_TRUE(true);
    }

    TEST(workplacePopulatorTest, overPopulationTest) {
        EXPECT_TRUE(true);
    }

    TEST(workplacePopulatorTest, noPopulationTest) {
        EXPECT_TRUE(true);
    }
}
